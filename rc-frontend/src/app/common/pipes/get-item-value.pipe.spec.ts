import { GetItemValuePipe } from './get-item-value.pipe';

describe('GetItemValuePipe', () => {
  it('create an instance', () => {
    const pipe = new GetItemValuePipe();
    expect(pipe).toBeTruthy();
  });
});
